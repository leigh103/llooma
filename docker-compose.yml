services:
  llooma:
    build: .
    ports:
      - "${PORT:-11003}:11003"
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./docs:/app/docs
      - ./config/apis:/app/config/apis
    restart: unless-stopped
